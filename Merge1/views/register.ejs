<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Live Chatting Platform</title>
    <!-- Font Awesome for Icons (though not used in this specific form, kept for consistency) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <style>
        /* --- Adopted CSS Variables from Login View --- */
        :root {
            /* Kept core colors for alerts */
            --color-bg-deep: #0f172a; 
            --color-red: #ef4444; 
            --color-green: #22c55e;
            
            /* Primary Theme Color (Purple) */
            --color-accent: #9333ea; /* Deep Violet */
            --color-accent-hover: #c026d3; /* Vibrant Magenta for Hover */
            
            /* White card specific colors (Dark text/borders on light background) */
            --color-card-text-dark: #1f2937;
            --color-card-muted-dark: #6b7280;
            --color-card-input-light: #f3f4f6;
            --color-card-border-light: #e5e7eb;
            
            /* Color for the main title (Black) */
            --color-title-dark: #000000;
        }
        
        /* --- Dynamic Gradient Background Styles (Copied from Login) --- */
        .dynamic-gradient-bg {
            background: linear-gradient(-45deg, #4c0099, #7c3aed, #f472b6, #38bdf8);
            background-size: 400% 400%;
            animation: gradient-flow 30s ease infinite; 
        }

        @keyframes gradient-flow {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        /* --- End Dynamic Gradient Styles --- */

        body {
            font-family: 'Inter', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background: var(--color-bg-deep); 
            color: var(--color-text-light);
            overflow: hidden; 
        }

        /* Applying the dynamic class to the body */
        body.dynamic-gradient-bg {
             background: linear-gradient(-45deg, #4c0099, #7c3aed, #f472b6, #38bdf8);
             background-size: 400% 400%;
             animation: gradient-flow 30s ease infinite; 
        }
        
        .login-card {
            /* Changed class name from .card to .login-card for consistency */
            background: #ffffff; 
            padding: 3rem; 
            border-radius: 12px; 
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2); 
            text-align: center;
            width: 380px; 
            border: 1px solid var(--color-card-border-light); 
            position: relative; 
            z-index: 10; 
            color: var(--color-card-text-dark); 
            /* Removed hover transition to match Login View */
        }

        h1 { 
            /* H1 for main title, black for contrast */
            color: var(--color-title-dark); 
            margin-top: 0;
            margin-bottom: 0.5rem;
            font-size: 2.25rem;
            font-weight: 800;
            letter-spacing: -0.05em;
        }
        
        .subtitle {
            color: var(--color-card-muted-dark); 
            font-size: 1rem;
            margin-bottom: 2.5rem; 
            display: block;
        }
        
        form { 
            /* Added padding and border-bottom to match Login View separation style */
            margin-bottom: 30px; 
            border-bottom: 1px solid var(--color-card-border-light); 
            padding-bottom: 30px; 
        }
        
        .input-group {
            position: relative;
            margin-bottom: 1.25rem; /* Added margin group for consistency */
        }

        input {
            width: 100%;
            padding: 1rem; 
            background-color: var(--color-card-input-light);
            color: var(--color-card-text-dark); 
            border: 1px solid transparent;
            border-radius: 8px; 
            box-sizing: border-box;
            font-size: 1em;
            transition: background-color 0.3s, border-color 0.3s, box-shadow 0.3s;
            margin: 0.5rem 0; /* Adjusted margin to fit the 3 inputs */
        }
        input::placeholder {
            color: #9ca3af;
        }
        input:focus {
            background-color: var(--color-card-border-light);
            border-color: var(--color-accent);
            box-shadow: 0 0 0 1px var(--color-accent); 
            outline: none;
        }
        
        .submit-btn {
            /* Button styled with purple accent */
            width: 100%;
            padding: 1rem;
            background-color: var(--color-accent); 
            color: white; 
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 700;
            margin-top: 1.5rem;
            font-size: 1.1em;
            transition: background-color 0.3s, transform 0.1s;
        }
        .submit-btn:hover { 
            /* Hover effect using magenta accent */
            background-color: var(--color-accent-hover); 
            transform: translateY(-1px);
        }

        .login-link { 
            /* Link styled with purple accent */
            font-size: 0.95em; 
            color: var(--color-accent); 
            margin-top: 30px; 
            display: block;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }
        .login-link:hover {
            color: var(--color-accent-hover);
            text-decoration: underline;
        }

        /* --- Message/Error styles for completeness --- */
        .message {
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 1.5rem;
            text-align: left;
            font-weight: 600;
        }

        .error-message {
            background-color: rgba(239, 68, 68, 0.15); 
            color: var(--color-red);
            border-left: 4px solid var(--color-red);
        }
        /* --- End Message/Error styles --- */

        @media (max-width: 450px) {
            .login-card {
                width: 90%;
                margin: 0 10px;
                padding: 2rem 1.5rem;
            }
        }
    </style>
</head>
<body class="dynamic-gradient-bg">

    <!-- Registration Card (Content, z-10) -->
    <div class="login-card">
        <h1>Group 40</h1>
        <span class="subtitle">Create your new account</span>
        
        <!-- Error Message Display -->
        <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
            <div class="message error-message">
                <%= errorMessage %>
            </div>
        <% } %>
        
        <form action="/auth/register" method="POST">
            <!-- Used input-group for consistency, although no special styling is applied yet -->
            <div class="input-group">
                <input type="text" name="username" placeholder="Username" required>
            </div>
            <div class="input-group">
                <input type="email" name="email" placeholder="Email Address" required>
            </div>
            <div class="input-group">
                <input type="password" name="password" placeholder="Password" required>
            </div>
            <button type="submit" class="submit-btn">Register</button>
        </form>

        <a href="/auth/login" class="login-link">Already have an account? Login</a>
    </div>
</body>
</html>
